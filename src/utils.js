export function createData(id, name, latitude, longitude, species, audio, moreInfo) {
    return {
        id,
        name,
        latitude,
        longitude,
        species,
        audio,
        moreInfo,
    };
}